(function installClientQuoteUi(){
'use strict';
function close(){document.getElementById('client-quote-dialog')?.remove();}
function parseLines(text){return text.split(/\r?\n/).map((line)=>line.trim()).filter(Boolean).map((line)=>{const [label,amount,basis,note,category]=line.split('|').map((v)=>v?.trim());return {label,amount:amount?Number(amount):null,currency:'USD',basis:basis||null,note:note||null,category:['firm','statutory','conditional'].includes((category||'').toLowerCase())?category.toLowerCase():'firm'};});}
function parseOptions(text){return text.split(/\r?\n/).map((line)=>line.trim()).filter(Boolean).map((line)=>{const [carrier,containerType,amount,transitDays,destinationCharges,indicativeEtd,scheduleStatus,remarks,recommended]=line.split('|').map((v)=>v?.trim());return {carrier,containerType,amount:Number(amount),currency:'USD',transitDays:transitDays?Number(transitDays):null,destinationCharges:destinationCharges?Number(destinationCharges):null,destinationCurrency:'USD',indicativeEtd:indicativeEtd||null,scheduleStatus:scheduleStatus||'Subject to booking confirmation',remarks:remarks||null,recommended:/^(yes|y|true|recommended)$/i.test(recommended||'')};});}
function payload(dialog){const template=dialog.querySelector('#cq-template').value;return {template,title:dialog.querySelector('#cq-title').value.trim()||null,pol:dialog.querySelector('#cq-pol').value.trim()||null,pod:dialog.querySelector('#cq-pod').value.trim()||null,placeOfDelivery:dialog.querySelector('#cq-delivery').value.trim()||null,terminal:dialog.querySelector('#cq-terminal').value.trim()||null,hsCode:dialog.querySelector('#cq-hs').value.trim()||null,dutyRate:dialog.querySelector('#cq-duty').value.trim()||null,customsExamNote:dialog.querySelector('#cq-exam').value.trim()||null,waitingTime:dialog.querySelector('#cq-waiting').value.trim()||null,destinationChargesNote:dialog.querySelector('#cq-dest-note').value.trim()||null,validity:dialog.querySelector('#cq-validity').value.trim()||null,includeCommercialNotes:dialog.querySelector('#cq-commercial-notes').checked,hiddenMarkupFlat:Number(dialog.querySelector('#cq-markup-flat').value||0),hiddenMarkupPct:Number(dialog.querySelector('#cq-markup-pct').value||0),services:parseLines(dialog.querySelector('#cq-lines').value),options:parseOptions(dialog.querySelector('#cq-options').value),notes:dialog.querySelector('#cq-notes').value.split(/\r?\n/).map((v)=>v.trim()).filter(Boolean)};}
function updateMode(dialog){const comparison=dialog.querySelector('#cq-template').value==='ocean_comparison';dialog.querySelector('#cq-service-wrap').hidden=comparison;dialog.querySelector('#cq-option-wrap').hidden=!comparison;}
function setValue(dialog,id,value){if(value!=null)dialog.querySelector(id).value=value;}
function fill(dialog,data){setValue(dialog,'#cq-template',data.template);setValue(dialog,'#cq-title',data.title);setValue(dialog,'#cq-pol',data.pol);setValue(dialog,'#cq-pod',data.pod);setValue(dialog,'#cq-terminal',data.terminal);setValue(dialog,'#cq-delivery',data.placeOfDelivery);setValue(dialog,'#cq-validity',data.validity);setValue(dialog,'#cq-dest-note',data.destinationChargesNote);setValue(dialog,'#cq-markup-flat',data.hiddenMarkupFlat??0);setValue(dialog,'#cq-markup-pct',data.hiddenMarkupPct??0);setValue(dialog,'#cq-hs',data.hsCode);if(data.services)dialog.querySelector('#cq-lines').value=data.services.map((x)=>[x.label,x.amount??'',x.basis??'',x.note??'',x.category??'firm'].join(' | ')).join('\n');if(data.options)dialog.querySelector('#cq-options').value=data.options.map((x)=>[x.carrier,x.containerType,x.amount??'',x.transitDays??'',x.destinationCharges??'',x.indicativeEtd??'',x.scheduleStatus??'',x.remarks??'',x.recommended?'yes':'no'].join(' | ')).join('\n');if(data.notes)dialog.querySelector('#cq-notes').value=Array.isArray(data.notes)?data.notes.join('\n'):String(data.notes);updateMode(dialog);}
async function loadSaved(dialog){const type=dialog.querySelector('#cq-source-type').value;const ref=dialog.querySelector('#cq-source-ref').value.trim();const status=dialog.querySelector('#cq-status');if(!ref){status.textContent='Enter a saved quote reference.';return;}status.textContent='Loading saved quote…';const response=await fetch(`/api/client-quotes/prefill/${encodeURIComponent(type)}/${encodeURIComponent(ref)}`);const data=await response.json();if(!response.ok){status.textContent=data.error||'Could not load quote';return;}fill(dialog,data);status.textContent=`Loaded ${ref}. Review the client-facing details.`;}
async function preview(dialog){const status=dialog.querySelector('#cq-status');status.textContent='Creating preview…';const response=await fetch('/api/client-quotes/preview',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload(dialog))});if(!response.ok){const data=await response.json();status.textContent=data.error||'Preview failed';return;}const html=await response.text();const win=window.open('','client-quote-preview');win.document.open();win.document.write(html);win.document.close();status.textContent='Preview opened.';}
async function download(dialog){const status=dialog.querySelector('#cq-status');status.textContent='Creating PDF…';const response=await fetch('/api/client-quotes/pdf',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload(dialog))});if(!response.ok){const data=await response.json();status.textContent=data.error||'PDF failed';return;}const blob=await response.blob();const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='client-quote.pdf';a.click();URL.revokeObjectURL(url);status.textContent='PDF created.';}
function open(event){close();const d=document.createElement('div');d.id='client-quote-dialog';d.className='simple-dialog-backdrop';d.innerHTML=`<section class="simple-dialog" role="dialog" aria-modal="true" style="max-width:960px"><div class="simple-dialog-head"><div><h2>Create client quote</h2><p>Load a saved quote or enter rates manually. Profit stays internal.</p></div><button class="simple-dialog-close" type="button">×</button></div><div class="row" style="margin-top:14px"><select id="cq-source-type"><option value="ocean">Ocean</option><option value="drayage">Drayage</option><option value="trucking">Trucking</option></select><input id="cq-source-ref" placeholder="Saved quote reference"><button id="cq-load" type="button" class="btn-sm">Load saved quote</button></div><div class="ship-update-grid" style="margin-top:14px"><label>Template<select id="cq-template"><option value="ocean_comparison">Ocean carrier comparison</option><option value="import_usa">Ocean import FCL, to USA</option><option value="import_canada">Ocean import FCL, to Canada</option></select></label><label>Title<input id="cq-title"></label><label>POL / origin<input id="cq-pol"></label><label>POD / destination<input id="cq-pod"></label><label>Container terminal<input id="cq-terminal"></label><label>Place of delivery<input id="cq-delivery"></label><label>HS code<input id="cq-hs"></label><label>Duty indication<input id="cq-duty"></label><label>Rate validity / basis<input id="cq-validity"></label><label>Hidden profit per rate<input id="cq-markup-flat" type="number" value="0"></label><label>Hidden markup %<input id="cq-markup-pct" type="number" value="0"></label></div><label>Customs examination note<input id="cq-exam"></label><label>Waiting-time clause<input id="cq-waiting"></label><div id="cq-service-wrap"><label>Service lines <span class="muted-inline">service | amount | basis | note | category</span><textarea id="cq-lines" rows="8"></textarea></label></div><div id="cq-option-wrap"><label>Carrier options <span class="muted-inline">carrier | equipment | rate | transit | destination collect | ETD | status | remarks | recommended</span><textarea id="cq-options" rows="8"></textarea></label></div><label>Destination charges note<input id="cq-dest-note"></label><label>Additional operational notes<textarea id="cq-notes" rows="3"></textarea></label><label style="display:flex;gap:8px;align-items:center"><input id="cq-commercial-notes" type="checkbox" checked style="width:auto"> Include concise commercial notes</label><div class="row"><button id="cq-preview" type="button" class="btn-sm">Preview</button><button id="cq-download" type="button" class="primary">Create PDF</button><span id="cq-status" class="status-inline"></span></div></section>`;document.body.appendChild(d);d.querySelector('.simple-dialog-close').addEventListener('click',close);d.addEventListener('click',(e)=>{if(e.target===d)close();});d.querySelector('#cq-template').addEventListener('change',()=>updateMode(d));d.querySelector('#cq-load').addEventListener('click',()=>loadSaved(d));d.querySelector('#cq-preview').addEventListener('click',()=>preview(d));d.querySelector('#cq-download').addEventListener('click',()=>download(d));if(event?.detail?.type)d.querySelector('#cq-source-type').value=event.detail.type;if(event?.detail?.refId){d.querySelector('#cq-source-ref').value=event.detail.refId;loadSaved(d);}else if(event?.detail){fill(d,event.detail);}updateMode(d);}
document.addEventListener('client-quote-open',open);
})();
